# deploy

CLI scaffold for an infra provisioner that uses the AWS and Cloudflare SDKs directly. State is tracked via resource tags. **Boilerplate only — no resource is actually provisioned yet.**

## Build

```bash
make build
./bin/deploy --help
```

## Test

```bash
make test
```
